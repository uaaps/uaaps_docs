{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://uaaps.github.io/uaaps_docs/schemas/bundle.schema.json",
  "title": "UAAPS Portable Bundle Manifest",
  "description": "Schema for bundle.json — the internal manifest inside a .bundle.aam portable bundle archive. Describes the pre-compiled platform-specific artifacts.",
  "type": "object",
  "required": ["format", "version", "package", "package_version", "target", "built_at", "checksum", "artifacts"],
  "additionalProperties": false,
  "properties": {
    "format": {
      "description": "Bundle format identifier. Always 'uaaps-bundle'.",
      "type": "string",
      "const": "uaaps-bundle"
    },
    "version": {
      "description": "Bundle manifest format version. Currently '1.0'.",
      "type": "string",
      "const": "1.0"
    },
    "package": {
      "description": "Package name (matches name field in package.agent.json).",
      "type": "string",
      "pattern": "^(@[a-z0-9][a-z0-9_-]{0,63}/)?[a-z0-9][a-z0-9-]{0,63}$"
    },
    "package_version": {
      "description": "Exact package version this bundle was built from.",
      "type": "string"
    },
    "target": {
      "description": "Target platform this bundle was compiled for.",
      "type": "string",
      "enum": ["claude-code", "cursor", "copilot", "codex", "universal"]
    },
    "built_at": {
      "description": "ISO 8601 timestamp of when this bundle was built.",
      "type": "string",
      "format": "date-time"
    },
    "checksum": {
      "description": "SHA-256 checksum of the bundle archive, prefixed with 'sha256:'.",
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "artifacts": {
      "description": "List of pre-compiled artifacts included in this bundle.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["type", "name", "path"],
        "additionalProperties": false,
        "properties": {
          "type": {
            "description": "Artifact type.",
            "type": "string",
            "enum": ["skill", "agent", "command", "rule", "hook", "mcp"]
          },
          "name": {
            "description": "Artifact name.",
            "type": "string"
          },
          "path": {
            "description": "Relative path to the artifact within the bundle archive.",
            "type": "string"
          }
        }
      }
    }
  }
}
